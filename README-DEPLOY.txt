BDL Sourcing — how to update the live app
1. Open the GitHub repo → Add file → Upload files.
2. Drag the WHOLE contents of this folder in (index.html, css/, js/, tests/).
3. Commit. GitHub Pages serves it within a minute. Same URL.
Rollback: upload the previous dated folder the same way.

SHARED STORAGE (b10): saves in the browser first, then row-by-row to the shared BDL Supabase project (src_ tables).
  One-time setup: run ~/Downloads/2026-09-13-BDL-SOURCING-SUPABASE.sql in the project's SQL editor. Until that is done the
  header pill says "Shared storage not set up" and every change queues in the browser, then sends itself once the tables exist.
Rules: js/rule1.js is FROZEN (9 Sep 2026). js/rule2.js is Rule 2 AND Rule 3 (same maths). js/rule4.js is VAT. js/queue.js is
  the new/better compare. Do not edit rule1 by hand.
Checks: open the page, add ?checks to the URL, and the known-answer tests run against the fixtures
        (fixtures are kept locally in bdl-sourcing-harness/fixtures, not in the repo).

Build: v1.2 · 2026-09-17 · b97 — Audit: pressing the verdict a row ALREADY has now reopens its picker. Marking the same verdict twice is a no-op internally — nothing to save — and the caller bailed out before anything redrew, so on a row already marked Discord, pressing 3 did literally nothing and PS/THC/FFB could never be reached again. That is what was still wrong after b96; Audit: Discord's second step (PS / THC / FFB) now appears for everyone. The six verdicts can be overridden by a list saved in your browser, and a list saved before that step existed was winning outright — so Discord had no reasons and nothing happened on the second click. A saved list may still say which verdicts exist and what they are called; how one behaves always comes from the code now, so no future change can be invisible to a browser holding an old list; Audit: judging only ever moves you DOWN the shelf. It used to search downward for the next unjudged row and, finding none, wrap round to row 1 — so one keypress teleported you to the top and you lost your place (Jack: "why does unsure take us to the top"). Now it settles on the last row and tells you how many are still waiting above; press G to go back to the first of them. A judged row's title and buttons also step back so your eye lands on the work, not the done; Audit, what a press feels like: judging a row now flashes that row once in the verdict's own colour and pops the chip in, over 420ms, then settles to its quiet judged tint. You see it land, you see it go quiet, and your eye is already on the row below. The mark expires, so passing the row again never replays it, and the whole thing is off under prefers-reduced-motion; Audit colours: the left stripe was on every row — 117 to-do rows in iris at half opacity with the odd orange among them — so it told you nothing. Now: to do has no stripe, a judged row wears its verdict's colour at full strength with a faint tint behind it, and the row you are on gets a wider bar and a lift so you never lose your place. Same rule as everywhere else: bright means a decision; Audit, the jumpiness (Jack: "isn't smooth at all - very very jumpy"): judging changes a row's height — six buttons appear, a reason row opens, both close again — and every row below it jumped. The row you are ON is now pinned to the same spot on screen across the re-render, so the page never moves under your hand. Smooth scrolling is switched off while a shelf is open (it turned every keypress into an animation you had to wait for), and the reason picker only opens on the row you are on — a judged row that never got a reason used to keep it open forever, two rows deep; Audit: Missed it can now say "Never found it" (key Y) — our filters never surfaced it, which is a different fault from "Never looked at" (it reached the list and nobody judged it). One points at a filter, the other at a pair of eyes. Other moves from Y to U; every other key is unchanged; Rule 1 handling is one number now: PREP_MISC £1.00 a unit instead of 60p prep + 40p misc, matching what Rule 2 already did (Jack, 17 Sep). Same money — verified against his own 62-lead EU run, every landed cost, sell price, profit, ROI and breakeven identical to the penny; Mera · 7-Day Drops £20+ all lines 20% is LIVE (Jack, 17 Sep): its rank month was still pinned to Sep 2025 so it has been re-pinned to Aug 2026, the last complete month, and the source switched on daily. A link or status you have changed yourself is left alone; Audit: the "Sellers on it" dash is fixed at the root — it was doing two jobs, "Keepa says nobody is listed" and "we never asked", and products saved before the offer count existed counted as loaded forever so the dash never cleared. Those now refetch themselves on the next details pass and read "loading…" until they do, or "none listed" when Keepa really has none; "On their shelf: before" now reads "Already there"; Audit tab strip rebuilt: a colour dot only where the count means something, empty verdicts fade back instead of shouting a coloured zero, the count is a proper badge that fills with the verdict colour on the tab you are on, and thin rules group it into where you are / what we already know / what you decided / everything; No confirmed figure is now read as a CEILING, not a gap (Jack: "something that doesn't even show 50 spm by amazon even if it says 40 drops, it isn't over 50 sales") — with his own exception: above 50 rank drops the figure has been LOST rather than the product being small (measured on his 1,404-row no-figure export: 60 of them, 4%), and those are treated as 50+. Under 50 a month, 10 drops or fewer and half the listing or less: marked 1 unit only when ROI is 50%+ AND profit £10+ a unit, otherwise leave it — the four numbers live in ONE_UNIT, one line to change; Demand now has a written order of resort (Jack: "confirmed sales > everything"): 1. Amazon's own bought-in-past-month, per option and never under 50, always believed. 2. no confirmed figure — this option's share of the family's reviews, from the Reviews columns in the export (PROVEN on the Logitech Lift family: real sales 1000/300/200/100/100 against shares 68.7/13.9/10.5/6.7/0.3%, same order, and the one miss is an option listed in March). 3. nothing else — the rank, which every option shares. New columns Option reviews, Option share %, Demand basis, Demand belongs to; a share from an option under six months old says so. Nothing is dropped or rescored; Variations, corrected after measuring the Logitech Lift family: "bought in past month" IS each option's own sales (grey 1,000, pink 300, left-handed 200) but the SALES RANK is the parent's, one number shared by all of them. So a bought-fed figure is now left alone and only a drops-fed one is flagged as the whole listing's; Audit: "Marking our own homework" — when a rival is selling something our own filters found and we never bought, the panel puts the sell price the rule promised against what it really goes for today, says whether the call held or the ROI was flattering, and works out what that buy would gross now; the row tile carries the ROI we quoted and says nobody said Yes; Audit: the 'Our lead' tile now NAMES the filter or brand that found it (electricals £60+, Logitech, A2A £10–40…) with a dot in that person's colour, so a rival's shelf reads as who-found-what before you read a word; Audit: Discord now asks WHICH one — PS, THC or FFB — the same picker Missed it uses (keys Q/W/E), so pressing 3 waits for your answer instead of jumping on; the picker wears the verdict's own colour, so orange still means Missed it and nothing else; Variations (Jack: "some spm are well off — if they are a var they aren't selling when we look"): tick Variation Count / Variation ASINs / Variation Attributes once in the Keepa column picker and every lead now says "1 of N" next to its demand, with a flag saying the figure belongs to the whole listing. Free, no tokens. Nothing is dropped for it — which option sells is read off the graph. On his 100 alert ASINs, 88 were one option of many and one family had 92; the step-by-step now tells VAs to tick those columns; CURRENCY FIX (Jack sent the amazon.es checkout that proved it): the Keepa API answers EU prices in EUROS while a Keepa CSV export is priced in the account's currency, so his EU exports are already in pounds. The fetched prices are now converted once before Rule 1 sees them — before this a euro price was read as pounds and every EU buy looked ~17% dearer than it is (Braun epilator: £77.06 landed against a real £68.91 basket). On his 100 EU alert ASINs this turns 46 leads into 62; Copy KPV link on the run toolbar (a Keepa Product Viewer link with every lead already loaded, for sending on); the run toolbar is sticky so Open in Keepa / Copy / Mark all seen follow you down a long lead list, and the page glides instead of jumping; Ultimate Ears added to the Logitech brand run (Keepa files it under its own brand with Logitech only as manufacturer, so the run never saw it — the Keepa link is built from that list, so the filter itself widens); New source 'EU alerts · price check' (Jack only, one-off): drop the UK Product Viewer export of ASINs that pinged in, press Get EU prices and Keepa fills the EU buy side at 1 token per ASIN per market (DE/FR/IT/ES), cached 12 hours, cost shown before anything is spent; Amazon's own price only and no Subscribe & Save, so profit is never flattered; Rule 1 is untouched — the fetched prices arrive in the exact shape a dropped CSV makes; Mera high-ticket is now Mera · electricals £60+, and Mera high-ticket £150+ is Mera · electricals £150+ (measured on the 12 Sep export: 136 TVs and monitors, 84 kitchen, 68 vacuums, 41 laptops, 40 printers, 40 phones, 26 coffee machines). A filter you renamed yourself is left alone; Audit: the product picture in the panel fits its box again (it was being cut in half: the box is a grid, the row was being sized by the 500px image instead of the 170px box, so 'contain' had nothing to contain against); Audit: orange now means Missed it and nothing else — 'We had it' and the 'Our lead' chip are slate, because they are history, not a decision; Audit: the SellerAmp button no longer sends a cost price (the Buy Box price is what the rival sells at, not what you pay; SellerAmp now opens on the ASIN alone, exactly as pressing S already did); Audit: a Next button on the row for the mouse, Amazon/Keepa/SellerAmp stay in view in the panel; Audit: the Keepa price graph loads in the panel for the product you are on; bigger, clearer answer buttons and site buttons; Audits landing: 39 rivals show as a sortable list (cards for a handful), ordered by how much of their shelf you already sell; Audit: Start picks the rival you overlap with most; order by sells a month, price, sales rank or seller count; sellers filter; seller count on every row; Audit rows rebuilt: long titles no longer run under the buttons, brighter readable text, the ASIN is a proper copy button, order and price/sales filters, the loading line says what it is doing with a bar; Audit: one Start button on the landing page, Next rival on every shelf and on the done card, add-a-shelf boxes tucked away; Audit: leads we had and never bought are marked Missed it automatically (one key overrides); Amazon / Keepa / SellerAmp are proper buttons on every row; "Keepa follows me" keeps one Keepa tab on whatever product you are on; Audit list calmed down: the six buttons only on the row you are working, colour only where it means something, bigger titles, no repeated shelf text, a proper prompt when nothing has pictures yet; Storefront audits: Jack-only page, a rival’s shelf judged one product at a time with keys 1-6 (Not lead · Unsure · Discord · WS · Joint lead · Missed it with a reason), an answer counts on every shelf, "was our lead, nobody said Yes" from our own runs, titles free from a Keepa export or ~1 token each, undo, bulk, end-of-audit summary; run 2026-09-16-BDL-SOURCING-AUDIT-SUPABASE.sql once; Hoover, Tassimo, GoPro, Corsair/Elgato, Skullcandy and SteelSeries now carry Jack's Keepa links and are Active; Microsoft, Staub and Xiaomi banned everywhere; Under £60 the sell price is never capped below the cheapest FBA offer live now (Jack, 16 Sep: Nescafé Decaf jar); Step-by-step now quotes the Keepa Export Data box (rows per page to max, All active columns, CSV, no currency symbols); owner/status/cadence/edit/pause/delete/Mark-all-seen are Jack only; Open in Keepa is the main button; AVM HQ links carry the VA name (#run=key&who=Suz) so the name prompt never shows; VA flow: your-day banner with Start, how-a-day-works panel, step-by-step guide with the how for every step (VAs default to it, Jack to the compact strip), Next due → on the run screen (head + after the toolbar), page scrolls to the leads when a fresh run lands; Run screen: To review / All leads / New / Better / Worse / Same buttons with counts, last-runs strip under the name, plain-English how-it-works line, saved-run banner with a Show-all button, Copy link for AVM HQ + All-time leads buttons, #leads=key deep link; Rule 3 VAT keyword fix (a coffee title naming beans/ground/instant/pods/bags/a weight stays 0% whatever else it says); SteelSeries on UK too; Lead history view (every stored lead, filter/sort any way, judge from there, CSV export, pulls the shared lead table); Brands list sort + type filter + count chips + owner/status pills editable in the row; Runs log filters (period, who, rule, source), sortable columns, roll-ups by source/day/person, click a run to open it; one colour per person; Five old ShiftTrack filters (Suz 20+, 7 Day 7-40, 7-50; Mera 7-Day £50+, £20+ all-lines) seeded as paused sources with their history and links; Open in Keepa marks the opened leads as seen; Run screen stacks for every source (file rows in a grid), no dead column; Rule 1 rows that lose money and no code fixes go to Dropped; Deep links #run=<key> and #due for AVM HQ tasks; Done-today rows green, paused grey; owner and cadence editable in the row; Run summary compacted and brightened; NETGEAR banned; Bialetti on all five markets; empty-queue wording matches the new-or-better rule; sales floor hint on hover; "vs yesterday" analysis (yesterday/today/change + best new, biggest improvement and fall); dates dd/mm/yyyy; one-file sources stack (no dead column); To-review = new or better than the last run only (unchanged/worse wait in Everything); floors hint readable; Brands list: due today/overdue rows red, done-today grey, cadence under status, due-first sort; Under £60 a lead needs 10% ROI, own price or with a code; Score-35 floor applies under £60 only (Mera shows everything); TANKING? chip when FBA slides month on month with 30+ sellers; Suz · A2A £10–40 (daily) on Jack's final link; Rule 2 leads must score 35+ (own or with a code); the rest go to the dropped list; £1/100%/1k scores 80; Under-£60 score: ROI and volume carry it (£1/100%/1k = 75); Amazon Basics, Clipper, Google blacklisted; duvets/bedding never-sell; Under-£60 score leans on ROI + profit a unit (Hogwarts 33→53); Suz · Amazon deep drops £10–40 seeded with the merged filter; A Rule 2 source takes several UK exports and merges them de-duped by ASIN; £/month figures gone (Demand sub-line, summary tile → ROI 20%+); Grocery/beauty rows get Boots/Superdrug/Tesco code potential; Marks Electrical/AO only offered on real electricals; BLS removed everywhere; All Testing sources go Active; list sorts due-now first; Runs log redesigned; stale summary cleared when opening a source with nothing to show; Keepa token balance in the header via the Cloudflare Worker (bdl-sourcing); Floors card moves under the run summary (no dead space), "Tanked" chip reads "FBA price falling"; A run source opens on its saved leads (fresh export re-runs); retailer names in full; brighter OA cell; link buttons; SPM floor wording; VAT rule is now called Rule 3 everywhere on screen (was Rule 4; the old Rule 3 folded into Rule 2 on 13 Sep); Electronics/computers/large appliances take the plateau model under £60 (Canon TS4150i); FBA floor = lower of the FBA 30d/90d averages; OA cell names the brand code (Shark 9%, Hoover 15%…); Rule 2 £60+ refit; Keepa inputs on lead states; who-are-you gate
  slot and no Step-4 merge box; OA chips just say 'Check OA · up to x% ROI' (retailers on hover).
Previous: v1.1 · 2026-09-14 · b20 — Rule 1 leads get a score (same formula, display only, the frozen maths is untouched) and sort by it;
  flag chips are short labels with the full text on hover; buy market shown as a flag; calmer palette (no coloured pills, ROI green/amber/red);
  dropped-reason rows list their ASINs on hover; OA chip only when it is worth ≥8% ROI.
Previous: v1.1 · 2026-09-14 · b19 — low-ticket sell: the 180d Buy Box average is ignored when it is a different price regime (>1.35× the 90d,
  e.g. a launch price) — L'OR pods £19.94 → £10.82 (a loss, dropped); low-ticket SCORE scale: £1,500/month and £6/unit saturate under £60
  (WoodWick candle 28 → 51). Mera and all 12 grocery calls unchanged. S&S 136 leads.
  Also b19 — run screen tightened: floors live in the drop card (no dead space on one-file runs), summary compressed,
  Keepa/SAS/Buy/Sell links under the product title (Links column gone), score + status in one column, 'OA check · you check' column with
  category-aware retailers (grocery: Boots/Superdrug/Tesco by eye; electricals: Currys/AO/Marks Electrical; general: Argos/JL/Robert Dyas).
Previous: v1.1 · 2026-09-14 · b18 — under-£60 sell model refitted on 13 of Jack's grocery calls (higher Buy Box 90/180d average, no uplift,
  capped at the FBA 90d average; all calls within ±9%): S&S 137 leads, Business 10, Mera unchanged.
  Round two of Jack's calls: Logitech within 2% of the frozen Rule 1 (no change); high-ticket: only FBA history proves a plateau now
  (FBM-only rows get +5%, not +25%) — Galaxy Book4 Pro £2,207 → £1,788; Mera 12 Sep 390 → 376, 11 Sep unchanged.
  Also b18: Rule 3 folded into Rule 2 (Jack: behaviours belong to the product, not the filter): the under-£50 sell
  model, S&S / Business / coupon buy adjustments and Rule 4 VAT all apply per row on every UK filter. Sources on rule 3 migrate to 2.
Previous: v1.1 · 2026-09-14 · b17 — RULE 3 SELL CHANGE (Jack's grocery calls): under £50 the sell price is the lower Buy Box 90/180-day average
  with no plateau uplift, capped at the Amazon 90-day average; £50+ keeps Mera's model. S&S 596 → 103 leads, Business 16 → 8. Rule 2 untouched.
  Never-sell categories (alcohol, fashion, shoes — editable in Settings, shared) drop leads on every rule. HP + Oral-B in the brand blacklist (live).
Previous: v1.1 · 2026-09-14 · b16 — run summary card now carries the story, the status pills and a detail block (why things dropped, leads by
  market or score band, ROI bands) so there is no dead space; flag links tick green once that country's export is in; every row has Buy <market>
  and Sell UK links; floors save quietly (no toast, re-log 2.5 s after typing stops, 0 = unset); 'Open all N in Keepa' when the view is ≤250;
  list: on-it tag under the status, 'manual export' instead of a token count, hover highlight.
Previous: v1.1 · 2026-09-14 · b15 — UK-buy Rule 1 leads carry an 'OA · Currys 5% → £x · y% ROI' chip (best price-matcher from the discount
  list, all of them on hover) plus the Currys/Argos/JL/Brand-direct confirm toggles; tables use Inter instead of the monospace (Jack: 'font is a bit
  shit'); links as a 2×2 grid; sales floor placeholder shows the rule's own 10/month.
Previous: v1.1 · 2026-09-14 · b14 — leads table redesign: ASIN sits under the title (both rules fit the card without scrolling at 1440),
  coloured market pills, ROI bands, coloured flags, tinted header, row hover; the story sits inside the Run summary card (no dead space);
  floors bar renamed 'Customise this source'.
Previous: v1.1 · 2026-09-14 · b13 — Rule 1 run screen: the merge/Viewer bar is visible from the start ('Step 4 happens here'), plain-text
  note that Keepa cannot take the marketplace from a link (switch the flag in Keepa per country); story repaints after verdicts; No rows readable.
Previous: v1.1 · 2026-09-14 · b12 — leads per page (50/100/200/all) with 'Open these N in Keepa' saying how many it opens; the run
  summary points at 'Mark all as seen' when unreviewed leads are carried over from the last run.
Previous: v1.1 · 2026-09-14 · b11 — readability + density pass on the leads table (brighter greys, bigger secondary text, tighter rows,
  chips capped at 4 + '+n'), Track button removed (Keepa's page has the Track tab), links next to the verdict, run summary told as a
  sentence (export → leads → to review today, and what changed vs last time), own lock never shown as 'someone else'.
Previous: v1.1 · 2026-09-13 · b10 — shared storage (Supabase src_ tables, local-first outbox), who-am-I dropdown, per-source in-progress lock,
  To-review queue (new + better since last verdict, 2p counts), ASIN blacklist with reason, brand blacklist with Jack approval,
  Rule 4 VAT (tea/coffee 0%, VA override chip, editable words), Rule 3 = Suz grocery/S&S/Business (same maths as Rule 2),
  Suz's three filters seeded (EU one parked, tea & coffee waiting for its link), per-source FLOORS (the customisable Filter & Sort,
  saved on the source, applied before the queue), brand + title stored with every lead, Settings regrouped with shared / this-browser tags,
  Track-at-£ note, Keepa 'low was' on WORSE rows. Checks: 41.
  Rule 3 locked on Suz's 13 Sep exports (S&S 2,520 → 596 · Business 179 → 16); Rule 4 word lists widened (syrups, perfumes, cosmetics, supplements = 20%). Checks: 54.
  Edit drawer redesigned 13 Sep 22:10: sections (What it is / Where it buys / Who and when / Keepa / Notes), rule + status as pick-one cards,
  owner + cadence one-tap, market chips, link field shows saved vs generated with Open, brand-only and filter-only fields hide, Delete in the footer.
  LIVE-TESTED 13 Sep 21:30 with two browsers against the real src_ tables (seed rows are in; test rows removed).
Previous: v1.0 · 2026-09-13 · b9 — Jack's real Keepa Finder links seeded for 15 sources; Corsair/Elgato, Ninja/Shark, SanDisk/Seagate/WD merged; Bialetti + Repken seller watch added; UK-only brand runs work off one Finder export
