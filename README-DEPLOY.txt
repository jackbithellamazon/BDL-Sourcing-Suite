BDL Sourcing — how to update the live app
1. Open the GitHub repo → Add file → Upload files.
2. Drag the WHOLE contents of this folder in (index.html, css/, js/, tests/).
3. Commit. GitHub Pages serves it within a minute. Same URL.
Rollback: upload the previous dated folder the same way.

SHARED STORAGE (b10): saves in the browser first, then row-by-row to the shared BDL Supabase project (src_ tables).
  One-time setup: run ~/Downloads/2026-09-13-BDL-SOURCING-SUPABASE.sql in the project's SQL editor. Until that is done the
  header pill says "Shared storage not set up" and every change queues in the browser, then sends itself once the tables exist.
Rules: js/rule1.js is FROZEN (9 Sep 2026). js/rule2.js is Rule 2 AND Rule 3 (same maths). js/rule4.js is VAT. js/queue.js is
  the new/better compare. Do not edit rule1 by hand.
Checks: open the page, add ?checks to the URL, and the known-answer tests run against the fixtures
        (fixtures are kept locally in bdl-sourcing-harness/fixtures, not in the repo).

Build: v1.2 · 2026-09-25 · b155 — GUEST MODE (Jack: "a guest mode so it doesn't actually save to Supabase at all, as I wanna run them all and check them"). Settings > Guest mode (Jack only). In: flush both outboxes (refuses if anything can't send), fresh cloudPull + all lead states + console days, snapshot every app key to IndexedDB (not localStorage — it can be MBs), then bdl-sourcing-guest {on}. While on, cloudEnabled() is false exactly like the localhost sandbox (nothing queued, sent or pulled), cloudReq refuses writes, the audit queue is a no-op, sign-in emails / Team edits / the lock switch refuse, and the amber bar can 'View as' Suz or Mera (the signed-in name is not forced). Out: wipe app keys, restore the snapshot, reload — the boot pull then brings in what changed meanwhile. The snapshot skips the sign-in session (Supabase rotates it), the Keepa caches, fx, theme and lock id. Other tabs follow via the storage event. Lock steps in HOW-TO now use Create new user + Auto Confirm (an invite lands on the project's main URL). 269 checks. Previous: b154 — DUE / LATE IN PLAIN WORDS, AND LEAD / NOT A LEAD IN THE KEEPA CONSOLE (Jack: "improve font and UI UX design of due now and overdue"; "anything else to improve and fix"; "want a way to do it on keepa console like for them to have the lead and not leads"). DUE: the pills say "9 days late", "Due today" and "First run" in the Mac system font with one dot, not "OVERDUE · 9D" in shouting caps; three strengths of the same red (late = filled, today = outline, first run = dashed); row tint quieter so 30 due rows are not one red wall (late rows keep the stronger tint and rail). The latest now sort first (9 days late above 1 day late — they used to be A–Z). FIXES: Jack's banner said "nothing is assigned to you yet · 30 due for others" — unassigned is his until he hands it out, so it now counts as his (and Mine includes it); the "Checked 72% · 281 of 393 leads judged" tile counted Seen stamps as judgements (live data: 460 of 461 verdicts were bulk Seen) — now "Looked at" with judged vs only-marked-seen; "Due today" is "Due now" with late / today / never run. KEEPA CONSOLE: after a Product Viewer export goes into Filter & sort, every row has Lead / Not a lead (Y / N on the row under the mouse; Not asks why in a floating list, keys 1–5, so no row grows), a From picker (which filter they came from), judged / leads / not counts, All · To judge · Leads · Not views (a row you just judged stays until you change view) and Copy leads. Saved under the person and the day in src_settings (kc:<Name>:<day>) — no new table; a VA pulls only her own days; Jack sees a Team card (last 7 days per person + latest leads). Deliberately NOT the run verdicts: a console call has no maths to compare, so it must not bury a lead the rules later find profitable. The console list uses the screen height now. 264 checks. Previous: b153 — THE LOCK, MAC TYPE, AND NOTHING MOVES WHEN YOU CLICK (Jack: "build it then please"; "smoother and fun to use — not rigid or buggy or jumpy — better fonts and text, remember Mac user"; "bulk edit is great on storefront audit — use it and make sure it works nice"). THE LOCK: a Team list in Settings (one email per person, typed by Jack, shared) decides every name — never a dropdown. Each browser records when its person signs in, so Jack can see who is in. A 'Sign-in required for everyone' switch, Jack's only, will not turn on unless Jack is signed in on that browser himself, so it can never lock him out. When it is on, a browser with nobody signed in shows the sign-in card and nothing else (the app is not drawn behind it). Signed in, you cannot pick another name, and ShiftTrack's who= links are ignored. Already-signed-in browsers keep working if Supabase is down. Localhost is always unlocked for testing. It is a front door, not a vault — the database key is in the page source like every app on the project. Tested: switch refused until Jack signed in; signed out while locked = sign-in card only; as Suz, cannot become Mera, sees only her 8 sources, a who=Mera link ignored, no Team or switch in her Settings. MAC TYPE: San Francisco first (Inter for everyone else); the monospace that covered 118 places — every number, header, pill and count — is now the system font with tabular figures, and JetBrains Mono is kept for ASINs only. Buttons get tabular figures too (Chrome's own button font was resetting them). Secondary text brighter on the dark theme (~2.6:1 to ~3.7:1). Pages fade instead of rising 8px. A tiny squeeze on every button while it is pressed (a transform, so nothing moves). Keyboard-only focus rings. NOTHING MOVES ON THE LEAD TABLE: measured on screen, (a) b144 sent every lead you answered to the bottom on each redraw — Y on row 1 threw it to row 63, page 2, out from under the mouse; anything answered in this sitting now holds its place — and (b) a No showed its five reasons AND the note box AND the name line, growing the row 15px and shoving every row below; the reasons now take that space until one is picked. Eight clicks in a row (N, reason, Y, N, M, reason, Y, N): 0 rows moved, 0px scrolled. BULK AUDIT, used end to end: the whole product picture ticks (not just the little box); X ticks the row you are on; the bar glides in; press 3 on five ticked rows and they all go Discord and STAY ticked so Q / W / E finishes all five (it used to clear the ticks and Q reached one row); Missed it opens its seven reasons on the bar; the toast carries an Undo button and waits 5 seconds; the toast always sits above the bar, measured from the bar's real height. 254 checks. Previous: b152 — THE AUDIT ROW HOLDS STILL WHEN YOU CLICK (Jack: "still very jumping when I click it"). b141 measured rows holding their place on the PAGE; this time every element in the clicked row and the next one was measured on the SCREEN, before and after a click, and four separate movements turned up. (1) A NAME CLASH: the leads page's "N of M judged" pill used the CSS class .judged — the same class every judged AUDIT row wears — so the moment you judged a row it took the pill's monospace font, grey colour and 11px size; every letter of the title, prices and buttons changed shape. That is why judged titles looked different in Jack's screenshot. The pill is .jpill now. (2) The six buttons sat 4px apart on the row you were on and 6px on every other row (a b68 rule), so each click slid them 8px sideways on two rows at once. (3) 'Missed it' has seven reasons; they wrapped to a second line and the row grew 30px. They fit one line now. (4) Judging the row near the bottom of the screen moved the focus to an off-screen row and the page scrolled 230px under the mouse. A mouse click never scrolls now; the keyboard still keeps its row on screen but parks it a third of the way down so the next few presses do not scroll. Also the verdict pill no longer clips ('Missed i'). Re-measured: six clicks in a row, one of each verdict — 0px scrolled, nothing moved, every row 148px; the only change is the reason chips replacing the buttons in the same slot, as designed. Each cause is pinned by a check. ALSO: the checks now run with the clock pinned to 24 Sep 2026. On 25 Sep four went red with no code changed — the Galaxy S26 options in the 12 Sep Mera export turned exactly six months old, so b105's rule now judges them on their own review share instead of the family's sales, and 19 left that old export's leads. The rule was right; the test was replaying a 12 Sep export with a 25 Sep clock. 243 checks. Previous: b151 — BULK AUDIT (Jack, 24 Sep: "add a way to bulk audit and click — a load of these are lead group, Discord, and I know which one"). Every row in the storefront audit now has a tick box on its picture. Tick one, shift-click another to tick the whole run between them, or cmd-click anywhere on a row. A bar pinned to the bottom of the screen then judges every ticked row in ONE press, including which Discord: 'Discord · PS / THC / FFB' are one click each. 'Missed it' opens its seven reasons in the bar. 'Tick all N shown' and Esc to clear. Q / W / E set the reason on every ticked row too, and the keyboard's old shift-arrow selection still works. One press is one batch, so a single U takes back the whole lot. The bar floats over the page, so ticking never moves a row: measured 0px of movement ticking five and judging them, every row 148px throughout. ALSO FIXED — the bug in Jack's screenshot of the same page: on a row judged Discord but not yet given a group, and not the row you were on, the 'Which Discord?' chips were drawn ON TOP of the six buttons (TRIP Dream Drops). b141's 'buttons on every row' rule was more specific than the rule that hides them while a row waits for a reason. The hide now outranks every variant, pinned by a check. 240 checks. Previous: b150 — INSTAX, EVERY MARKET (Jack, 23 Sep: "instax add - all eu's and uk"). A new brand run, Rule 1, UK plus Germany, France, Italy and Spain, on Jack's own Keepa filter kept verbatim: brand instax, Amazon down 8% or more on its 90-day average, products only, sorted by sales rank then monthly sold. The rank month rolls itself on open like every other filter. It lands UNASSIGNED like every brand since b126, so only Jack sees it until he hands it to someone; it reaches Supabase on the first open after the push, so ShiftTrack will count it as one more unassigned brand run. The Run via Keepa API button works on it too — the only part of the filter the API cannot take is the rank month, the same as every other source. Every 3 days, matching Canon. 235 checks. Previous: b149 — LIVE PRICES ARE ALWAYS TODAY'S (Jack: "but the live price still needs to be new though doesn't it?"). He was right, and b148 had it wrong. Measured on B0FJM3MZ9T: the 1-token call DOES refresh Amazon's own price (the A2A buy price), the lowest new price, the rank and monthly sold — but the live Buy Box, the live FBA offer, the live FBM offer and the total offer count all need the 6-token offers data. b148 carried the live FBA offer forward from the cache as if it were today's. Now only AVERAGES come off the shelf, never a live price: on a 1-token refresh the live 3P prices are left blank and the rules fall back on the averages, which is Rule 1's own 'never a spot price' principle. Then every product that comes out as a LEAD is pulled in full straight away and the rules run again, so anything anybody acts on carries today's competition. The confirm step never goes under the 250 floor — if the balance will not stretch it says which leads still carry old averages and tells you to check the live offer on Keepa. Proved live on the Ninja air fryer: the refresh came back with the live FBA price blank and the averages intact for 1 token; the lead confirm brought the live FBA offer back at £264.99 for 6. 233 checks. Previous: b148 — SIX TOKENS DOWN TO ONE (Spense in the Discord, passed on by Jack: "Caching is VERY VERY important when using keepa. Average prices aren't going change much over a few days"). A full Keepa row costs 6 tokens because it needs the offers data; a stats-only call is 1 token and comes back with exactly 13 columns empty — measured, not guessed: the Buy Box 30/90/180 averages and high, out-of-stock %, Amazon's share, the FBA current and 30/90 averages, monthly sold last known and its date, and the two review columns. Everything else is identical. So an ASIN we already hold now costs 1 token to refresh what actually moves, with the averages coming off the shelf. Free if the whole row is under a day old. THREE DAYS, NOT NINETY: measured on Jack's own consecutive exports, the 90-day averages drift about 0.25% a DAY — 0.23% over one day, 0.60% over three, 1.73% over seven, with 8% of products moving more than 5% in a week. Three days keeps the typical error near half a percent; ninety would be a fifth out. The run is priced off what it already holds before you confirm, the cache is capped at 6,000 rows oldest-first so it cannot eat the browser, and the top-up keeps the ORIGINAL average date so cached averages still expire on time. Proved live on two Ninja ASINs: a full pull was 12 tokens, the refresh was 2, with the averages merged and the rank and prices genuinely fresh. 231 checks. Previous: b147 — IF IT IS PROFITABLE WE STILL WANT IT (Jack, 22 Sep: "but if we had an OA sell price that is fine - remember it's A2A but if we find something profitable then we still want it"). A product Amazon is not selling in the UK or any of the four EU markets used to be thrown away with 'Amazon not selling anywhere'. It had already passed demand and it already had a sell price — the only thing missing was somewhere to buy it, which is a job for a retailer, not a reason to bin it. Those products now leave the run as OA TARGETS: a panel above the leads, biggest seller first, showing what it sells for, what it sells a month, the breakeven buy price and the price to beat for 20% ROI, with the whole list as a CSV. The dropped list says the same thing in words. They are never leads and never scored — a lead has a buy price and these have not got one yet — so nothing in the leads table moves: Logitech still 112, ASUS still 38. Proved on the two Ninja ASINs Jack sent: the Foodi FlexDrawer sells 1,000 a month at £268.83 and needs buying under £168.89 for 20%, the Prestige coffee machine sells 200 at £212.63 and needs £132.93. 226 checks. Previous: b146 — ITALY, FRANCE, GERMANY WHEN THE PRICE IS MATCHED (Jack, 22 Sep: "order of cheapness on EU if price matched - italy france germany - unsure where spain sits on this just yet", then "if spain is still cheaper or UK is cheaper then use that - if anyone is cheaper than the others even by a penny use that one"). The order is a tie-break on a MATCHED price and nothing else: a penny cheaper anywhere wins, including Spain, including the UK. Two wrong cuts got measured out of it first - applied over the UK and over a cheaper Spain with a £1 window it sent two Logitech leads to a dearer country (ES £16.47 -> FR £17.38) and pushed one out of the run; a 25p window was still paying to follow an order. Now: EU only, half a penny of rounding, UK never displaced, Spain deliberately not in the order until Jack knows where it sits. Measured against the behaviour before any of this (Italy preferred within £1): Logitech 111 and ASUS 38 unchanged, nothing moved; Bialetti 22 unchanged with exactly one lead moving DE -> FR at an identical £16.56 landed and an identical 25.6% ROI, which is the rule doing precisely its job. It lives in euPick() with four checks. 222 checks. Previous: b145 — THE MORNING'S LIST STANDS, AND BETTER SAYS HOW MUCH BETTER (Jack: a second run the same day — "yeah, shouldn't really be ran 2 times a day really if I'm honest"; and "how are they more profitable — want to work on that"). The day's list is now set by the FIRST run of the day and only ever grows: a second run adds anything new it finds and takes nothing off, however the numbers have moved since this morning. Verified on two real Mera exports run back to back: 230 in the morning, all 230 still there after the second run, 26 added, 256. HOW MUCH BETTER: the BETTER pill now carries the gain — +£40.98 in green for a pound or more, +6p in grey for pennies — because they are not the same news. Measured on the 11→12 Sept Mera pair: 25 leads came back as "more profitable", 9 of them worth £1+ a unit and 13 worth under 25p, including one still LOSING £6 a unit that improved by 6p. That measurement is now a check, so any change to the rule shows up as a number. The rule itself is unchanged and still flags every improvement however small — that was the b10 instruction and it is Jack's to change. 218 checks. Previous: b144 — TODAY'S LIST STAYS ALL DAY, WITH WHO DID WHAT (Jack: "the 83 that was in the queue needs to be there for the day and marked done by x person"). The queue was a live filter: answer a lead and it vanished, so you could not see what you had done, who had done it, or how much was left — and a reload lost the list you started with. Today's queue is now the day's list. It is fixed the moment the run is logged and every lead on it stays visible until tomorrow. Answered ones drop to the bottom, greyed with a green edge, wearing the name of whoever answered them. The header reads "Today's list 19 to do · 5 done · Jack 3 · Suz 2" and a small bar shows "5 of 24 answered today". Reopening the source later the same day brings back the same list with the same progress; opening an older run from Last runs deliberately still shows that run as it was. Verified with two people answering on one run. 215 checks. Previous: b143 — SO IT CANNOT HAPPEN AGAIN, AND YESTERDAY IS READABLE (Jack: "what happened, what is the fix and improvement to make sure it never happens again — I want to see history as I might want to check yesterday's leads"). THE GUARD: every verdict now records HOW it was written — the row, the keyboard, or a bulk button by name — and a check reads js/brands.js itself and fails if any code other than "Mark all as seen" writes a Seen verdict. b142's bug was a button writing 82 verdicts as a side effect of looking; that shape is now a failing test, not a surprise. The label is kept in this browser only, because src_verdicts has no such column and sending one it does not know is what broke the audit sync in b109. A row that was marked seen says which button did it. YESTERDAY: a run now records WHICH leads needed a look and which had fallen off, not just how many, so reopening it answers "what was I looking at yesterday". Click any date in Last runs: it opens that run read-only, the To review tab shows exactly what was in the queue that day, and the bar reads "390 products in, 29 leads out, 29 new, 24 needed a look that day · 5 judged since (1 Yes)". Today's run now joins Last runs the moment it finishes — it used to need the source reopening before you could click back into it. 213 checks. Previous: b142 — OPENING LEADS IS NOT JUDGING THEM (Jack: "why only 83 and then I opened KPV and it only shows 1 now — wtf, super super buggy. Unless I upload new filters I should be able to see what I need to run"). Found it: "Open all N in Keepa" was ALSO marking every lead it opened as SEEN. That was his own instruction in b56, when opening them in Keepa WAS the review — but he now opens them in the Product Viewer to look, so the queue emptied under him. On the Philips run the numbers match exactly: 83 to review, 1 of them already judged, so 82 were stamped SEEN and "to review" fell to 1. Opening now marks nothing at all. Y / N / M still judge a row, "Mark all as seen" still sets the baseline, and the Open button says so on hover. PUTTING BACK WHAT IT TOOK: a Jack-only "Put N back in the queue" button sits next to "Mark all as seen" whenever a source has a batch of SEEN marks — it clears that newest batch and the leads return to To review. It names the day when it was not today, it can never touch a Yes, No or Maybe, and the removal goes to Supabase too so it does not come back on the next sync. Verified on a 29-lead run: 28 marked seen dropped the queue to 1, the button read "Put 28 back in the queue", and pressing it restored all 28 with a real Yes and its note untouched. 210 checks. Previous: b141 — THE STOREFRONT AUDIT STOPS MOVING (Jack: "storefront audit — still very jittery", "still very jumpy whenever I press anything"). The cause was one line of CSS: the six verdict buttons were revealed on :hover and on the row you were on, so a row grew about 44px as the mouse crossed it and again as you pressed a key, and every row below it jumped. b92 and b99 answered that by CORRECTING the scroll afterwards, which is why it nudged twice and never felt right. The buttons are now on EVERY row, always, in the same place — the row you are on differs by colour, never by layout. Measured over ten presses on a 14-row shelf: every row 148px before and after, and 0px of movement on every press. Same rule as the lead table learned in b128. Also: a press now repaints the list, the panel and the counts instead of rebuilding the whole page; the reason chips (Discord, Missed it) take the buttons' place instead of opening a new line underneath; the "You sell this too" chip stays after you judge, so the row cannot change height; the right-hand column is a fixed width, so a verdict pill landing never squeezes the title; and the product name and the numbers each get reserved space, so nothing is truncated. DEAREST FIRST NOW MEANS IT (Jack: "how is highest ticket first showing a 16 pound lead?"). The order is frozen on purpose so rows do not leap about while you judge, but it was frozen BEFORE the prices arrived, so it ranked a shelf where nearly every price was still 0. It now re-freezes the moment the numbers THAT sort reads land, and never when you judge. A PRICE ON MORE OF THEM (Jack: "sell price isn't going on all of them"). It only read the Buy Box, then Amazon, and showed nothing at all when a listing had neither. It now falls back to the cheapest live third-party offer, tags which one it is (3P / AMZ), and where there is genuinely no offer it says "no price on Keepa" instead of leaving a blank that reads as broken. ONE ANSWER PER PRODUCT, AND THE ROW SAYS SO (Jack: "are ASINs I press Not lead on saving, so another storefront has it down as not a lead too?"). Yes — that has always been the design, and there is no 90-day expiry. What was missing is the row admitting it: a verdict you gave on another shelf now reads "Not lead · from <that rival>" with the date. 207 checks. Previous: b140 — THE LEAD TABLE FITS THE LAPTOP (Jack: "keep improving smoothness and easy to use"). Measured, not guessed: on a 1366px laptop the lead table was 1,410px wide in a 1,269px space, so the page scrolled sideways and the OA check column hung off the right; at 1280 it was worse. Causes, each fixed: the verdict note box (220px) forced a 244px Verdict column (now 150, note box fills the cell); the level chips' number was being sized by ".ltbl td.num b" at 14.5px bold, so six chips came out 85x23 one under the other, 150px of every row (now 74x17, two a line, £100+ chips drop the pence); Chrome takes a cell's max-width as its width and never shrinks it, so Product is sized per screen (340 / 300 / 260 / 222); Sell and Score are pinned so auto layout cannot rob them; Demand and Buy £ subs no longer wrap to three and four lines; the "FBA avg may be stale" note wraps under the chips instead of widening Sell by 60px; a No row's five reason chips were 126px one under the other — once a reason is picked only that chip stays (click it to change), unpicked they are chip-sized (81px). Result at 1366 / 1280 / 1536: no sideways scroll, table 1,267 / 1,186 / 1,437 in 1,269 / 1,183 / 1,439, rows average 172px on Suz's list (same as before, with 140px more table on screen). KEYS CARRY ON: Y / N / M / ↓ at the last row of a page moves to the next page (↑ at the top goes back); 64 leads at 50 a page used to stop dead at row 50 with nothing saying there were 14 more — the pager now says "12 more on the next page — ↓ carries on". ALL DONE: when the last visible lead is judged the pill reads "64 of 64 judged ✓ all done" and the Done — next due button lights up. 201 checks. Previous: b139 — YOUR API LOOK IS NOT HER RUN (Jack, asked whether an API run counts as the VA's run: "not until they worked it"). A Keepa API run is now saved as Jack's own look, never as the source's run: no src_runs row (so ShiftTrack never sees it), the due date does not move, and the VA's next export compares against her own last export, not against what Jack saw. Verdicts given on the look are kept. The look sits first in Last runs as a dashed amber "API" chip, stored in this browser only (bdl-sourcing-api-looks, one per source, newest), reopens read-only, and an amber bar above the leads says what it is and is not; Back to the last run clears the API rows out of the slots. Also: ukDate now renders a full ISO stamp in local time, so every chip and bar shows the UK clock, not UTC (an hour behind all summer). 198 checks. Previous: b138 — RUN VIA KEEPA API, JACK ONLY (Jack: "only Jack myself can run it via API pls — we can get scheduled to work too soon but think my token bucket will run out"). On any source's run screen, beside Open in Keepa, Jack gets "Run via Keepa API · priced first". It asks Keepa for the filter's products, builds the same rows a CSV export would (stats + offers + rating = every column the rules read: Buy Box averages and high, FBA / FBM averages, out-of-stock %, Amazon's share of the box, review count, drops, monthly sold, fees, weight, variations) and runs the same rules — no export. Nothing is spent before you say yes: it counts first (about 11 tokens), then shows "316 products today · about 1,908 tokens · balance 1,249 → 253 after · floor 250". When the whole list will not fit it offers the top slice that will — "run the FIRST 164 instead for about 996 tokens" — Keepa's own order, best first; under 25 it refuses. Rows are cached for a day, so a second run of the same list costs only the count. Brand runs across five markets pull the EU prices too (1 token per market per product). The mapping is checked against the same two products' CSV export: Buy Box 90d, FBA 90d and FBM 90d within 2% two days apart; Buy Box high, monthly sold, FBA offers, out-of-stock %, fees, variation count and root category exact. What the API cannot give: a variation's own review share (it is judged on the family's drops and says so) and Business tiers. The real price: about 6 tokens a product, so a full Mera list is ~3,300 — this is for Jack's own runs, not the VAs' dailies, and the button never shows to anyone else. 196 checks. Previous: b137 — THE RUN TOOLBAR IS ONE ROW (Jack: "improve smoothness, UI/UX design and visually"). It was ten controls over two rows. Now: Open in Keepa on the left, Done — next due on the right, and everything else — Copy lead ASINs, Copy KPV link, Review sheet, CSV, Dropped list, Mark all as seen — behind one More menu that closes on Escape, on a click outside, or when you pick something. The judged pill and Jack's Check EU prices button sit beside More. Every button kept its id, so nothing rebinds. 192 checks. Previous: b136 — PROVED IT, DID NOT ASSUME IT (ShiftTrack asked whether the four brand runs Jack has already assigned — MSI and Gtech to Mera, Bialetti and SteelSeries to Suz — keep their owners when b126's "all brands unassigned" migration runs). They do: that migration only clears a brand whose owner is empty or the old seeded "VAs" pool, never a person Jack has picked. Simulated the whole push against the list the cloud holds today and it lands as Mera 7, Suz 10, Jack 1, VAs 1, 18 unassigned, with mera-200plus added paused and both Mera filters renamed — nothing a person set is touched. Pinned as a check so it cannot regress. 192 checks. Previous: b135 — A LOGITECH UK LEAD IS AN OA LEAD (Jack: "Logitech is multi-buys on the brand website, not Amazon — so be extra more lenient on UK A2A even if it's a small loss, tell them to go looking at the Logitech website and OA price matches for Discord"). The promos are on logitech.com, so judging one against Amazon's price is the wrong test. A UK Logitech lead is now kept when the 10% stand-in brings it within 5% of break-even (about -14% at Amazon's price), and the thin-lead bar no longer judges it on Amazon's price at all. The row says: "OA, NOT AMAZON: buy it from logitech.com - they nearly always have a multi-buy on (buy 2 get £10, buy 3 get £50, it changes). Check the site, then Argos and Currys for a price match, then Discord it." On the 9 Sep Logitech run: 91 leads -> 112, the 21 extra all showing their real loss against Amazon (-9% to -12%) with the promo line beside it. The brand site lives on the Settings discount row, so any other brand that runs promos on its own site can be set up the same way. 191 checks. Previous: b134 — LOGITECH UK BUYS GET THE BENEFIT OF THE DOUBT (Jack: "for Logitech if it's UK A2A be more lenient — they nearly always have extra % or promos, buy 2 get £10 off, the most recent was buy 3 get £50 off, it's so random"). Keepa cannot see a multi-buy promo, so a Logitech UK lead is now judged with a 10% allowance alongside the Argos / Currys price match, and the row says "if Logitech has a promo on (they usually do): £44.99 -> £6.24 / 14%" plus a flag telling the VA to check logitech.com, then Argos and Currys. It is worded as a promo, never as a code, and the headline profit is still the honest one at Amazon's price — it only keeps a thin lead alive for a human to look at. On the 9 Sep Logitech run: 78 leads -> 91. The allowance lives in the Settings discount list marked "promo", so any other brand that behaves this way can be given one without touching a rule. AMAZFIT IS NO LONGER BLOCKED ANYWHERE (Jack, 21 Sep): it was in the excluded-brand list of Mera's £150+ filter. Removed from that filter and checked across all 11 filters that carry a brand block — every other blocked brand survived intact. Mera electricals 19 Sep: 237 -> 238. 190 checks. Previous: b133 — "VAs" MEANS BOTH OF THEM. b132 hid everything a person does not own, which also hid the one source owned by "VAs" (the Repken seller watch) from both VAs — the label says VAs, so it should be theirs. Now: your own sources yes, anything owned by "VAs" yes, the other VA's no, unowned no, and Jack sees everything. 187 checks. Previous: b132 — EACH VA SEES ONLY HER OWN (Jack: "Mera shouldn't see Suz's and Suz shouldn't see Mera's"). One rule, used by the list, the week's KPI tiles, the next-due banner and the lead history: Jack sees everything; anyone else sees the sources that are theirs and nothing else; a source with no owner belongs to nobody yet, so it is Jack's until he hands it out. Measured in the sandbox: Jack 37 rows, Mera 5 (all hers), Suz 8 (all hers), neither sees a brand run. A link someone was sent still opens — being sent a source is different from browsing other people's. MERA'S "200+ · 10% DROP" IS IN, PAUSED (Jack: "get it added but currently under paused"). Her best filter, 2.26 leads a run over 31 runs, had never been added — it was blocking the ShiftTrack side from archiving her old filter library. The link was recovered from the 9 Sep unsaved-filters file and matched on three things: Amazon 30-day average £200+, Amazon and Buy Box both down 10%+ vs 90 days, rank month pinned to Mar 2026. 187 checks. Previous: b131 — A RUN CAN NEVER BE SAVED WITHOUT A NAME (found by the ShiftTrack side: 13 of 35 runs in Supabase have a blank "who"). Judging always asked who you are; dropping an export never did, so a run could be recorded with nobody on it and ShiftTrack could not say what Suz ran today. Now the export is held, the question is asked, and the drop replays itself the moment a name is picked — verified: dropped with no name = 0 runs saved, picked Suz = the run went through as hers. Old blank rows are left alone; a blank who now means "before 21 Sep". THE TWO MERA FILTERS ARE NAMED FROM THEIR ACTUAL KEEPA PAYLOAD, not from my notes: b126 called them "everyday £60–£150" and "big-ticket £150+", and decoding the links today shows the first has NO upper limit and is restricted to Mera's chosen categories (laptops, hoovers, TVs, monitors, coffee), while the second is a £150–£2,000 band across everything. Now "Mera · chosen categories £60+" and "Mera · anything £150–£2,000". Keys unchanged, so every ShiftTrack link still works. 185 checks. Previous: b130 — NOTHING ON THE TABLE MOVES OR FADES (Jack: "it just moved and jittered"). Measured again on a verdict click: the table top moves 0px, nothing above it changes height, the row stays. What was left was the level chips brightening and dimming as the mouse crossed each row, and that fade replaying after every click because the table redraws — a shimmer that reads as jitter. Gone: the chips are one constant shade, and every transition on the leads table is off, so the frame after a click is identical to the one before it apart from the verdict you set. 183 checks. Previous: b129 — SMOOTHNESS PASS (Jack: "keep working and fixing it - improve UI/UX, smoothness, user experience"). Measured first: a verdict click takes 23 ms, the table redraws in 10 ms, the page does not scroll and the row stays where it was — the table itself was already smooth; the jitter was the hover growth fixed in b128. Then three things that were not smooth: THE HEADER STAYS PUT on a wide screen (1280px+) — # / Score / Product / Verdict / Buy / Sell / Profit / ROI pinned at the top while a 236-row list scrolls under it (the lead wrapper and the sideways-scroll wrapper both clipped it; they let it through on wide screens now). Y / N / M SHOW THEIR COLOUR ON HOVER before you commit. THE EMPTY "NOTHING NEW TO REVIEW" STATE HAS A BUTTON — "See all 236 leads" — instead of telling the VA to go and find the All leads tab. Also verified in the sandbox: no console errors on any page, and a normal run spends 0 tokens. 183 checks. Previous: b128 — THE JITTER IS GONE (Jack: "very jittery, not smooth at all"): the price levels appeared on hover, so every row grew 130px and shrank again as the mouse crossed it — the whole table moved under the cursor. They are always there now and the row never changes height; the chips are dimmed until you are on the row. Short codes (BB90 · F30 · FBM · HIGH, full name in the tooltip) keep them to two lines, so a row is 181px instead of 270px. SIGNING IN IS IN SETTINGS (Jack: "where is magic link for them"): it only showed on the first-visit gate, so anyone who had already picked a name never saw it. Settings → Who am I now shows whether you are signed in, and when Jack is signed in he can send Suz or Mera their link from his own screen — Supabase emails an address he has already invited. EU PRICES WITHOUT AN EXPORT (Jack: "a Keepa tracker for all EU prices without export on drops — sometimes EU stuff is profitable in the UK, very very rarely though"): on any UK run, "Check EU prices · N leads · N tokens" asks Keepa what the leads cost in DE, FR, IT and ES and runs the brand-run maths — EU buy, UK sell, landed — then lists the ones that work. Jack-only, tick rows to check only those, the cost is shown and confirmed before anything is spent, and prices are cached so checking again is free. Measured: a normal run still spends 0 tokens. Also: "Start with Acer (—'s)" now reads "(unassigned)". 183 checks. Previous: b127 — LOOK AT A PAST RUN (Jack: "somewhere they should be able to view past ones"): every date in the Last runs strip is now a button. Click one and that day's run opens — what went in, what came out, and what everyone has decided since, with a bar saying "The run from Fri 18/09 · Jack · 521 products in, 337 leads out, 1 judged since. Nothing here changes what runs today" and a Back to today button. A lead that has since dropped off the list still shows, marked "no longer a lead — kept for the record", so the day is complete. AND THE NaN IS GONE: the Acer screen read "35 products in the Viewer -> NaN sell 10+ a month" on a stored run, because those counts were never saved. Runs now save their own counts, and a figure that was never recorded reads as a dash instead of NaN. 183 checks. Previous: b126 — SIGN IN WITH A ONE-CLICK EMAIL LINK (Jack: "you need to do logins ... logins should local browser save ... they should be under my login so I can send them links again"). js/auth.js: type your work email, Supabase sends a link, clicking it lands you back here signed in, and the session is kept in that browser so a VA signs in once on her machine. No passwords exist. The accounts live in Jack's own Supabase project, so he invites, re-sends or removes from the dashboard. An address he has not invited is refused ("Signups not allowed for otp — ask Jack to invite this address"). Signing in decides WHO YOU ARE, replacing the name picker, so a verdict can no longer carry the wrong person; it does NOT yet decide what you can read — every data call still uses the shared key, so a failed sign-in can never stop the app working, and the name picker stays underneath. Two things Jack must do first: add the live URL to Authentication -> URL configuration -> Redirect URLs, and invite Suz and Mera. BRANDS BELONG TO NOBODY (Jack: "put all brands onto n/a so I can choose - I don't want VAs seeing all of the stuff yet"): all 22 brand runs are unassigned; Jack hands each one to a person in the owner box and only then does it show in that VA's day. NAMES: "Mera · electricals £60+" and "£150+" read the same at a glance — now "Mera · everyday £60–£150" and "Mera · big-ticket £150+". 182 checks. Previous: b125 — MSI WAS NEITHER ACTIVE NOR PAUSED: its seed row had no status and no owner, so it never counted as active and its status box was blank. Set to VAs / active like every other brand run. 178 checks. Previous: b124 — CANON ADDED as a brand run (Jack's list of 20 Sep: Canon, Hoover, Lenovo, Logitech, MSI, Shark/Ninja, Philips, Skullcandy, Tefal — the other eight were already sources). All five markets, every 3 days, VAs, active; a saved list picks it up on load. 178 checks. Previous: b123 — EXPORTS COST NOTHING, AND A VARIATION GOES BACK ON THE FAMILY'S DROPS (Jack: "if it's under 50 confirmed sales we use Keepa drops, you know this" / "we use exports so it should cost 0 tokens"). b122 dropped an option whose share was unknown and asked Keepa on every drop; both undone. An option with no share of its own is a lead on the family's drops and says so on the row ("FAMILY DROPS · OWN SHARE UNKNOWN (9 OPTIONS)"). A run spends no tokens. Above the leads a yellow bar counts those leads and, for Jack only, offers "Ask Keepa · 20 tokens": one token per option, cached a week, a confirmed figure replaces the family drops, no figure leaves it on them and says "KEEPA HAS NO FIGURE FOR THIS OPTION". JUDGE WITH THE KEYBOARD: click a row (or ↓ ↑ / J K) to make it the active one, Y / N / M mark it and move to the next unjudged row, Esc clears; a "2 of 236 judged" pill sits in the toolbar. The rules stamp the review shares themselves now, so the checks apply the same share ladder the page always did: Mera 11 Sep 272 -> 231, 12 Sep 269 -> 260, UK-only 271 -> 262, electricals 19 Sep 272 -> 237. 178 checks. Previous: b122 — A VARIATION HAS TO SELL ON ITS OWN (Jack, on the M190: "it's a var and looks like it doesn't sell"). Until now a variation whose own share of the family's sales could not be worked out - its siblings were not in the export, or nobody has reviewed anything yet - was quietly handed the whole family's drops (a 9-option AOC monitor with 35 family drops counted as 35 of its own). Now such a row is "share unknown": when the export lands the page asks Keepa for that exact option's confirmed monthly sales, one token each, cached a week, only for rows that would otherwise be leads; a confirmed figure (or a last-known one in the past year) keeps it, no figure drops it with the reason on the Dropped list, and the yellow note says "Keepa checked 14 variations · 14 tokens · 0 confirmed, 14 no figure". Filter exports rarely carry the siblings, so Mera's lists move most: 11 Sep 272 -> 203, 12 Sep 269 -> 235, electricals 19 Sep 245 -> 217, laptops 50 -> 35 after the live check (14 tokens, none confirmed). An export without the review columns is left alone (b111 refuses it anyway). stampShares moved into parse.js and the rules call it themselves, so the checks and the page agree. 178 checks. Previous: b121 — NO S&S SEEN, SAY SO (Jack: "no, keep - should say"): the maths stays as it is, but a grocery / health / beauty / pet / baby product where Keepa shows no Subscribe & Save gets "NO S&S SEEN · CHECK THE PAGE" on the row (Rule 2 chip, Rule 1 flag), because that is where the missing 15% usually hides. 174 checks. Previous: b120 — THE VELOCITY BAR SLIDES (Jack: "I'd happily sell something at 200 spm at under £1 profit - most of the time you're missing sub & save or the prep fee is over-exaggerated"): straight lines between the points instead of steps. 50/mo = 10% or £3, 100/mo = 9% or £3, 200/mo = 7.5% or £1, 300+/mo = 6% or £1, under 50 = 20% or £3. Counts move by one or two: Logitech 77 -> 78, Mera UK-only 269 -> 271, Mera 11 Sep 271 -> 272, Mera 12 Sep 267 -> 269, Suz S&S 46 -> 47. 174 checks. Previous: b119 — THE VELOCITY BAR (Jack: "for the lower spm it needs to be higher profit or higher ROI to include it - the faster the spm the more I am happy with a low profit and lower ROI as it flies"): one sliding bar on both rules, from his six calls on Suz's list. 300+/mo needs 6% ROI or £1 a unit; 100-299 needs 9% or £3; 50-99 needs 10% or £3; under 50 is the b103 slow-seller rule, 20% or £3, unchanged. Judged on the best of the headline, the high-end sell and the price match, the same three that let a lead in. It replaces Rule 2's flat 10% under £60 and Rule 1's headline-only slow-seller test. Counts: Logitech 9 Sep 86 -> 77 (M240 £0.17 at 1000/mo, G203 £1.24 at 50/mo), ASUS 34 -> 38 and Mera UK-only 266 -> 269 because the high-end sell now counts for the slow-seller test too, Mera 11 Sep 280 -> 271, Mera 12 Sep 278 -> 267, Suz S&S 49 -> 46, Suz Business 6 -> 4. The row says "THIN FOR 100/MO · NEEDS 9% OR £3" on Rule 2 and the Dropped list says it on Rule 1. Jack has ticked the Last Known columns in Keepa. 173 checks. Previous: b118 — ONLY JACK TEACHES THE SELL MODEL (Jack: "probably just me"): a VA tapping a level still sets the sell for that lead, but only Jack's taps count towards the five-picks-at-70% rule that makes a level the app's own pick for a shape (LVL_TEACHERS in rule2.js). 171 checks. Previous: b117 — ONE SELL AND THE £100 BAR ARE ON (Jack: "just improve the rules"). Every lead on every rule now gets its sell price from the one model built on Jack's 38 graph calls (37 within 10%), and anything selling at £100+ must reach 8% ROI or £15 a unit at its best (Amazon's price, the brand code or the assumed 5% match). Two guards went in first: under £60 no level above 1.35x the Buy Box 90d average is taken (the 14 Sep old-price-regime rule; L'OR pods stay a loss at £11.47), and a sell capped at the Buy Box high says so, so Rule 1's capped count still works. Baselines moved and each carries its reason in tests/checks.js: Logitech 9 Sep 96 -> 86, ASUS 55 -> 34 (20 of the 21 were under the bar at their best), Mera UK-only 310 -> 266, Mera 11 Sep 354 -> 280, Mera 12 Sep 345 -> 278, Suz S&S 52 -> 49, Suz Business 8 -> 6. Also: the level chips stay tucked away until you are on the row, and tapping one keeps the page where it is. 171 checks. Previous: b116 — THE LEVELS ON THE ROW (Jack: "custom, dynamic and non-restrictive"): under the sell price on every lead, both rules, the six price levels Keepa gives for that product - Buy Box 90d, Buy Box 180d, FBA 30d, FBA 90d, FBM 90d, Buy Box high - each with its value and the seller count. Tap one and it is the sell for that lead (same as typing it, profit and ROI re-work, shared with everyone); tap it again to clear. The level the model is using is lit. THE APP LEARNS: each tap is stored with the product's shape (big pack / few sellers / Amazon owns the box / Amazon dips); once a shape has 5 picks with 70% on one level, the one-sell model uses that level for that shape and the row says "you take FBA 30d on this shape 9 of 11 times". STALE FBA NOTE: where the FBA 90d average sits 10%+ above the Buy Box average with 4 or fewer FBA sellers (the Dell 15, 20 Sep: £788 with nobody near it) the row says "FBA avg may be stale · check the graph". One sell and the £100 bar stay OFF. 38 of Jack's graph calls are in the checks (37 within 10%). Fixtures added: random-2026-09-19.csv, laptops-2026-09-19.csv. 171 checks. Previous: b115 — ONE SELL PRICE, BUILT AND SWITCHED OFF (Jack: "we need a custom, dynamic and non-restrictive sale price - every graph and ASIN is unique"). sellPick() in rule2.js prices a product by its shape, from 23 prices Jack read off the graph on 18-19 Sep: under £60 with 8+ FBA sellers above the box = the FBA 30-day level (G305 £40, Sharpie £14.50, Yankee £21); under £60 with a few sellers = Buy Box 90d +6% capped at FBA 90d; £60+ where Amazon owns the box and is rarely out of stock = Buy Box 90d +8% capped at FBA 90d (G923 £253, G515 £108, Braun £475-500, SanDisk £230); £60+ where Amazon dips or drops out (Buy Box OOS over 4%) = Rule 2's third-party caps (Shark, Siemens £600, Ninja, G PRO £88). Scored on his 23 calls: Rule 1 6.1% mean error, Rule 2 6.5%, one sell 3.7% and within 10% on 22 of 23 (the miss is the Lift at £67, a 3P holding the box under £60). THE £100+ BAR, BUILT AND SWITCHED OFF (Jack: "even with code, anything over £100 needs 8% ROI or £15 profit"): judged on the best of Amazon's price, the brand code and the assumed 5% match, both rules. Measured: Mera electricals 19 Sep 300 leads -> 362 with one sell (the extra 64 are 3P-holds-the-box products Rule 2 had priced at today's box; 55 of them under 8% and £15) -> 245 with both on; Mera 12 Sep 345 -> 278; Suz A2A 48 -> 54; Logitech 71 -> 65. Switches: UNIFIED_SELL.on and BAR100.on in rule2.js. 167 checks. Previous: b114 — BOSCH IS ALL MARKETPLACES (Jack, 19 Sep): the Bosch brand run now asks for UK · DE · FR · IT · ES Finder exports plus the UK Viewer, like Logitech; a saved Bosch row is migrated once. Also carried, switched OFF: BR.UNIFIED_SELL_60 in rule1.js (Rule 1 taking Rule 2's sell model at £60+). Jack's 18-19 Sep calls showed neither model wins everywhere: when Amazon owns the Buy Box (G923 80%, G515 99%, Buy Box out of stock under 4%) Rule 1's +8% is right; when Amazon dips or drops out (Lift 57% / 23% OOS = £67, G PRO 82% / 11% OOS = £88, Shark, Siemens, Ninja) Rule 2's third-party caps are right. The switch stays off; the split model is not built until more calls land. 163 checks. Previous: b113 — PRICE MATCH: BRAND STORES ARE KNOWN, RETAILERS ARE ASSUMED (Jack: "unless we know it's a price match at the place we have to go, it's a 5% — Hoover is fine as it's the brand, Shark is fine, but LG we might have at Currys, might at Marks, most likely Currys / Argos"): a brand-store code (Hoover Direct 15%, Shark 9%) counts at its rate; an electrical retailer the VA has NOT confirmed for that product is assumed at Currys / Argos 5%, not at its own rate (Marks Electrical 8%, AO £10); once the VA ticks the retailer on the row it counts at its real rate. Both rules. GTECH IS NOT A CODE (Jack: "where is Gtech discount ahaha"): its 19% is Amazon Business tiers, which Keepa's Business Discount column already carries when real, so a Business entry is never a price-match scenario and never widens the "needs % off" allowance. DELL added to the list with no rate ("Dell direct usually has some offer on — check at purchase"). Measured on the 12 Sep Mera export as UK-only: 307 -> 310 (+6 Hoover via Hoover Direct, -2 Acer laptops, -1 AOC monitor that only worked on Argos 6%); Logitech back to 96. Rule 2 Mera 11 / 12 Sep: 355 -> 354 and 346 -> 345, the one loss on each being the Gtech Duo bundle that needed 13.9% off and only stayed on the Business tier. 163 checks. Previous: b112 — ONE DISCOUNT LIST (Jack: "go then"): Rule 1 now reads the same Settings discount list as Rule 2 — brands and price-matchers, by category, the list Jack edits in Settings. Rule 1 had carried its own copy of three retailers and 30 brands since 8 Sep and it had drifted: Hoover 10 vs 15, Acer 15 flat vs 15 full-price-only / 5 on a match, no Tefal, no Shark, no Gtech, no Marks Electrical. Measured on the 12 Sep Mera export as UK-only: 307 -> 316 (+11 Hoover / Gtech / Marks Electrical price-match leads, all negative at Amazon's price and flagged A2A->OA; -2 Acer laptops that only ever worked on the flat 15%); Logitech 96 -> 97 (the K950 keyboard at 7% via Marks Electrical). rule1.js changed on Jack's instruction; its built-in pairs remain only as the fallback when the Settings list is not loaded. The "UK buy: check OA retailers (…)" flag now names the real retailers and rates for that product's category. 161 checks. Previous: b111 — EXPORT ERROR, NOT A WARNING (Jack: "should say error - pls tick these columns and name them"): a UK export missing any of the six demand-ladder columns now stops the run. The file chip goes red and the results area says "Export error — FILE is missing N columns the demand rules need. In Keepa's column picker tick: … Then export again." Files built from the API (EU alert list) are never blocked; PRICE-MATCH LINE SHOWS THE MONEY (Jack: "sometimes there is no price match and extra % off"): Rule 2 keeps judging a lead at Amazon's price, the price you can always buy at, and the "→ with Currys 5%" chip now reads "→ 11% ROI · £13.42 WITH CURRYS 5%" instead of a bare score. Rule maths unchanged. 159 checks. Previous: b110 — RANK MONTH ROLLS BY ITSELF (Jack: "ye"): Suz's A2A £10-40 filter was still pinned to September 2025 (202509), a year stale, and 25 other filters sit on August 2026. The saved link is untouched; the link that opens now carries the last complete month, every time, for every filter; MISSING COLUMNS ARE SAID ON THE FILE: the UK export chip now lists which of the six demand-ladder columns the export lacks (Variation Count / ASINs, Reviews Rating Count / Review Count - Format Specific, Monthly Sold (Last Known) and its Date) and says every variation is being judged on its family's figures until they are ticked; API RUNS ADDED, NOT USED (Jack: "add it in but not using yet"): js/apirun.js turns a saved Keepa finder link into a Product Finder API selection and can count a source's matches through the Worker. Measured 18 Sep: 10 tokens a query + 1 per 50 results + 1 per ASIN for stats, so a Suz run is ~335 tokens; brand blocks and the rank month cannot be sent to the API (tested) and are applied after the fetch. APIRUN.on is false; nothing in the app calls it. 157 checks. Previous: b109 — The one-year safety net (Jack: "has to have a confirmed spm in the last 365 days if the review % puts it under 10 spm"): when the review share would drop a variation under the 10-a-month floor, one Amazon-confirmed figure at any point in the past year keeps it, flagged "reviews put it at 5/mo, but Amazon confirmed 50/mo on DATE". Both rules. It can only keep a lead, never remove one; Confirmed sales that lapsed still count (Jack: "if it's had monthly sales it should stay"). Tick Monthly Sold (Last Known) and Monthly Sold Date (Last Known) under Monthly Sales Trends: if today's bought-in-past-month is blank but Amazon confirmed a figure in the last 90 days, that figure counts as confirmed on both rules. The Shark Matrix Plus (last confirmed 50/mo on 17 Jul, blank today) is back as a lead at 23.6% ROI / £63.72; the VA step-by-step now lists every column to tick; Review-share demand on EVERY rule (Jack: "var from reviews should be on all of them"): Rule 2 — Suz's and Mera's filters — now judges a variation with no confirmed figure on the family's drops x its own share of the reviews, exactly as Rule 1 has since b105. Confirmed beats everything. Measured on the 17 Sep exports: tea & coffee 4 -> 4, Mera electricals 322 -> 311 and 312 -> 307, the 1,404-row filter 17 -> 14; nothing added, and every removal is on the Dropped list with its reason; SYNC FIX: the amber "Audit tables not created yet" banner was wrong — the tables exist. Product rows carried two columns src_products does not have (offers, asked); Supabase rejected them with a message containing "schema cache"; the app read that as missing tables, switched audit sync off, and left every verdict queued behind a row that could never send. A missing column is no longer treated as a missing table, each table is sent only its own columns (the offer count goes in fba, where it belongs), and the stuck queue sends itself on the next load. Also b105: a variation with no confirmed figure is judged on the family's drops x its own share of the reviews — confirmed still beats everything, new options keep the family's drops; Variations: a lead with no confirmed figure of its own now names the sibling that really sells, on the row and in a Family seller column — "the family's sales are Fabric Blue 64GB (1,000/mo confirmed) — this option has no figure of its own". A label, never a gate. Needs the Variation and Reviews columns ticked in the Keepa export; Rules interlinked and a slow-seller change, both Jack's. (1) Rule 1 now takes its VAT from Rule 3, the same engine every other rule uses — it had its own seven-word coffee/tea list and never asked Rule 3, so one product could be taxed two ways depending on which run found it. (2) A slow seller (under 50 a month) now needs £3 a unit OR 20% ROI, was £5 or 15%. Measured on every run: it only adds leads, never removes one, and every one it adds makes £3+ a unit — the G321 Black is back. A stricter "£3 AND 10%" was tried and rejected because it removed £90-£153/unit high-ticket leads; Rule 1 rows now have a "your £" sell box, like Rule 2 always had. Type the price you read off the graph and the profit and ROI re-work through Rule 1's own fees; the rule's price stays on the row as "rule said £47.51" for comparison, the typed price is shared with the team, and clearing the box puts it back. Rule 1's formula is not changed — on 8 Sep Jack settled that the discounted-Amazon exception is the graph's job, and until now a brand run gave the graph nowhere to put its answer (G321 White: rule £47.51 = 7.2% ROI; typed £51.50 = £4.95, 16%); Import: Keepa exports ONLY the page on screen. An export that lands on exactly one of Keepa's page sizes (20, 50, 100, 200, 250, 500 or 1,000 rows) now shows an amber warning on the file itself, telling you to max out rows per page and export again. Across ~150 exports this had cut two short unnoticed, one of them the SanDisk/Seagate brand run at exactly 50; Storefront audits: an Order control on the rivals page. Seven ways to ask the list a different question — you sell the most of theirs, most were our leads, fewest left to judge, most left to judge, biggest shelf, least recently audited, name A-Z. Works on the cards and the list, and it remembers what you picked; Audit: the jitter. b92's scroll pinning ran on EVERY redraw and stored its result as the next target, so when the Keepa graph landed in the panel a moment later and the page grew, each redraw nudged the scroll again chasing a target it could no longer reach. The correction is now armed only by an actual keypress, runs once after the browser has laid the page out, then disarms — redraws nobody asked for leave the scroll completely alone; Audit, THE crash: b94 wrote auView.landed={a:asin} inside a function whose parameter is asins, so every single verdict press threw a ReferenceError after saving the verdict but before redrawing. The answer went in, the screen never moved, and it looked exactly like "Discord has no second step". Nothing in the checks called that function, so 129 of them passed while the one thing the audit does all day was broken; there is now a check that reads the judge path's own source and fails on a name that is not in scope; Audit: pressing the verdict a row ALREADY has now reopens its picker. Marking the same verdict twice is a no-op internally — nothing to save — and the caller bailed out before anything redrew, so on a row already marked Discord, pressing 3 did literally nothing and PS/THC/FFB could never be reached again. That is what was still wrong after b96; Audit: Discord's second step (PS / THC / FFB) now appears for everyone. The six verdicts can be overridden by a list saved in your browser, and a list saved before that step existed was winning outright — so Discord had no reasons and nothing happened on the second click. A saved list may still say which verdicts exist and what they are called; how one behaves always comes from the code now, so no future change can be invisible to a browser holding an old list; Audit: judging only ever moves you DOWN the shelf. It used to search downward for the next unjudged row and, finding none, wrap round to row 1 — so one keypress teleported you to the top and you lost your place (Jack: "why does unsure take us to the top"). Now it settles on the last row and tells you how many are still waiting above; press G to go back to the first of them. A judged row's title and buttons also step back so your eye lands on the work, not the done; Audit, what a press feels like: judging a row now flashes that row once in the verdict's own colour and pops the chip in, over 420ms, then settles to its quiet judged tint. You see it land, you see it go quiet, and your eye is already on the row below. The mark expires, so passing the row again never replays it, and the whole thing is off under prefers-reduced-motion; Audit colours: the left stripe was on every row — 117 to-do rows in iris at half opacity with the odd orange among them — so it told you nothing. Now: to do has no stripe, a judged row wears its verdict's colour at full strength with a faint tint behind it, and the row you are on gets a wider bar and a lift so you never lose your place. Same rule as everywhere else: bright means a decision; Audit, the jumpiness (Jack: "isn't smooth at all - very very jumpy"): judging changes a row's height — six buttons appear, a reason row opens, both close again — and every row below it jumped. The row you are ON is now pinned to the same spot on screen across the re-render, so the page never moves under your hand. Smooth scrolling is switched off while a shelf is open (it turned every keypress into an animation you had to wait for), and the reason picker only opens on the row you are on — a judged row that never got a reason used to keep it open forever, two rows deep; Audit: Missed it can now say "Never found it" (key Y) — our filters never surfaced it, which is a different fault from "Never looked at" (it reached the list and nobody judged it). One points at a filter, the other at a pair of eyes. Other moves from Y to U; every other key is unchanged; Rule 1 handling is one number now: PREP_MISC £1.00 a unit instead of 60p prep + 40p misc, matching what Rule 2 already did (Jack, 17 Sep). Same money — verified against his own 62-lead EU run, every landed cost, sell price, profit, ROI and breakeven identical to the penny; Mera · 7-Day Drops £20+ all lines 20% is LIVE (Jack, 17 Sep): its rank month was still pinned to Sep 2025 so it has been re-pinned to Aug 2026, the last complete month, and the source switched on daily. A link or status you have changed yourself is left alone; Audit: the "Sellers on it" dash is fixed at the root — it was doing two jobs, "Keepa says nobody is listed" and "we never asked", and products saved before the offer count existed counted as loaded forever so the dash never cleared. Those now refetch themselves on the next details pass and read "loading…" until they do, or "none listed" when Keepa really has none; "On their shelf: before" now reads "Already there"; Audit tab strip rebuilt: a colour dot only where the count means something, empty verdicts fade back instead of shouting a coloured zero, the count is a proper badge that fills with the verdict colour on the tab you are on, and thin rules group it into where you are / what we already know / what you decided / everything; No confirmed figure is now read as a CEILING, not a gap (Jack: "something that doesn't even show 50 spm by amazon even if it says 40 drops, it isn't over 50 sales") — with his own exception: above 50 rank drops the figure has been LOST rather than the product being small (measured on his 1,404-row no-figure export: 60 of them, 4%), and those are treated as 50+. Under 50 a month, 10 drops or fewer and half the listing or less: marked 1 unit only when ROI is 50%+ AND profit £10+ a unit, otherwise leave it — the four numbers live in ONE_UNIT, one line to change; Demand now has a written order of resort (Jack: "confirmed sales > everything"): 1. Amazon's own bought-in-past-month, per option and never under 50, always believed. 2. no confirmed figure — this option's share of the family's reviews, from the Reviews columns in the export (PROVEN on the Logitech Lift family: real sales 1000/300/200/100/100 against shares 68.7/13.9/10.5/6.7/0.3%, same order, and the one miss is an option listed in March). 3. nothing else — the rank, which every option shares. New columns Option reviews, Option share %, Demand basis, Demand belongs to; a share from an option under six months old says so. Nothing is dropped or rescored; Variations, corrected after measuring the Logitech Lift family: "bought in past month" IS each option's own sales (grey 1,000, pink 300, left-handed 200) but the SALES RANK is the parent's, one number shared by all of them. So a bought-fed figure is now left alone and only a drops-fed one is flagged as the whole listing's; Audit: "Marking our own homework" — when a rival is selling something our own filters found and we never bought, the panel puts the sell price the rule promised against what it really goes for today, says whether the call held or the ROI was flattering, and works out what that buy would gross now; the row tile carries the ROI we quoted and says nobody said Yes; Audit: the 'Our lead' tile now NAMES the filter or brand that found it (electricals £60+, Logitech, A2A £10–40…) with a dot in that person's colour, so a rival's shelf reads as who-found-what before you read a word; Audit: Discord now asks WHICH one — PS, THC or FFB — the same picker Missed it uses (keys Q/W/E), so pressing 3 waits for your answer instead of jumping on; the picker wears the verdict's own colour, so orange still means Missed it and nothing else; Variations (Jack: "some spm are well off — if they are a var they aren't selling when we look"): tick Variation Count / Variation ASINs / Variation Attributes once in the Keepa column picker and every lead now says "1 of N" next to its demand, with a flag saying the figure belongs to the whole listing. Free, no tokens. Nothing is dropped for it — which option sells is read off the graph. On his 100 alert ASINs, 88 were one option of many and one family had 92; the step-by-step now tells VAs to tick those columns; CURRENCY FIX (Jack sent the amazon.es checkout that proved it): the Keepa API answers EU prices in EUROS while a Keepa CSV export is priced in the account's currency, so his EU exports are already in pounds. The fetched prices are now converted once before Rule 1 sees them — before this a euro price was read as pounds and every EU buy looked ~17% dearer than it is (Braun epilator: £77.06 landed against a real £68.91 basket). On his 100 EU alert ASINs this turns 46 leads into 62; Copy KPV link on the run toolbar (a Keepa Product Viewer link with every lead already loaded, for sending on); the run toolbar is sticky so Open in Keepa / Copy / Mark all seen follow you down a long lead list, and the page glides instead of jumping; Ultimate Ears added to the Logitech brand run (Keepa files it under its own brand with Logitech only as manufacturer, so the run never saw it — the Keepa link is built from that list, so the filter itself widens); New source 'EU alerts · price check' (Jack only, one-off): drop the UK Product Viewer export of ASINs that pinged in, press Get EU prices and Keepa fills the EU buy side at 1 token per ASIN per market (DE/FR/IT/ES), cached 12 hours, cost shown before anything is spent; Amazon's own price only and no Subscribe & Save, so profit is never flattered; Rule 1 is untouched — the fetched prices arrive in the exact shape a dropped CSV makes; Mera high-ticket is now Mera · electricals £60+, and Mera high-ticket £150+ is Mera · electricals £150+ (measured on the 12 Sep export: 136 TVs and monitors, 84 kitchen, 68 vacuums, 41 laptops, 40 printers, 40 phones, 26 coffee machines). A filter you renamed yourself is left alone; Audit: the product picture in the panel fits its box again (it was being cut in half: the box is a grid, the row was being sized by the 500px image instead of the 170px box, so 'contain' had nothing to contain against); Audit: orange now means Missed it and nothing else — 'We had it' and the 'Our lead' chip are slate, because they are history, not a decision; Audit: the SellerAmp button no longer sends a cost price (the Buy Box price is what the rival sells at, not what you pay; SellerAmp now opens on the ASIN alone, exactly as pressing S already did); Audit: a Next button on the row for the mouse, Amazon/Keepa/SellerAmp stay in view in the panel; Audit: the Keepa price graph loads in the panel for the product you are on; bigger, clearer answer buttons and site buttons; Audits landing: 39 rivals show as a sortable list (cards for a handful), ordered by how much of their shelf you already sell; Audit: Start picks the rival you overlap with most; order by sells a month, price, sales rank or seller count; sellers filter; seller count on every row; Audit rows rebuilt: long titles no longer run under the buttons, brighter readable text, the ASIN is a proper copy button, order and price/sales filters, the loading line says what it is doing with a bar; Audit: one Start button on the landing page, Next rival on every shelf and on the done card, add-a-shelf boxes tucked away; Audit: leads we had and never bought are marked Missed it automatically (one key overrides); Amazon / Keepa / SellerAmp are proper buttons on every row; "Keepa follows me" keeps one Keepa tab on whatever product you are on; Audit list calmed down: the six buttons only on the row you are working, colour only where it means something, bigger titles, no repeated shelf text, a proper prompt when nothing has pictures yet; Storefront audits: Jack-only page, a rival’s shelf judged one product at a time with keys 1-6 (Not lead · Unsure · Discord · WS · Joint lead · Missed it with a reason), an answer counts on every shelf, "was our lead, nobody said Yes" from our own runs, titles free from a Keepa export or ~1 token each, undo, bulk, end-of-audit summary; run 2026-09-16-BDL-SOURCING-AUDIT-SUPABASE.sql once; Hoover, Tassimo, GoPro, Corsair/Elgato, Skullcandy and SteelSeries now carry Jack's Keepa links and are Active; Microsoft, Staub and Xiaomi banned everywhere; Under £60 the sell price is never capped below the cheapest FBA offer live now (Jack, 16 Sep: Nescafé Decaf jar); Step-by-step now quotes the Keepa Export Data box (rows per page to max, All active columns, CSV, no currency symbols); owner/status/cadence/edit/pause/delete/Mark-all-seen are Jack only; Open in Keepa is the main button; AVM HQ links carry the VA name (#run=key&who=Suz) so the name prompt never shows; VA flow: your-day banner with Start, how-a-day-works panel, step-by-step guide with the how for every step (VAs default to it, Jack to the compact strip), Next due → on the run screen (head + after the toolbar), page scrolls to the leads when a fresh run lands; Run screen: To review / All leads / New / Better / Worse / Same buttons with counts, last-runs strip under the name, plain-English how-it-works line, saved-run banner with a Show-all button, Copy link for AVM HQ + All-time leads buttons, #leads=key deep link; Rule 3 VAT keyword fix (a coffee title naming beans/ground/instant/pods/bags/a weight stays 0% whatever else it says); SteelSeries on UK too; Lead history view (every stored lead, filter/sort any way, judge from there, CSV export, pulls the shared lead table); Brands list sort + type filter + count chips + owner/status pills editable in the row; Runs log filters (period, who, rule, source), sortable columns, roll-ups by source/day/person, click a run to open it; one colour per person; Five old ShiftTrack filters (Suz 20+, 7 Day 7-40, 7-50; Mera 7-Day £50+, £20+ all-lines) seeded as paused sources with their history and links; Open in Keepa marks the opened leads as seen; Run screen stacks for every source (file rows in a grid), no dead column; Rule 1 rows that lose money and no code fixes go to Dropped; Deep links #run=<key> and #due for AVM HQ tasks; Done-today rows green, paused grey; owner and cadence editable in the row; Run summary compacted and brightened; NETGEAR banned; Bialetti on all five markets; empty-queue wording matches the new-or-better rule; sales floor hint on hover; "vs yesterday" analysis (yesterday/today/change + best new, biggest improvement and fall); dates dd/mm/yyyy; one-file sources stack (no dead column); To-review = new or better than the last run only (unchanged/worse wait in Everything); floors hint readable; Brands list: due today/overdue rows red, done-today grey, cadence under status, due-first sort; Under £60 a lead needs 10% ROI, own price or with a code; Score-35 floor applies under £60 only (Mera shows everything); TANKING? chip when FBA slides month on month with 30+ sellers; Suz · A2A £10–40 (daily) on Jack's final link; Rule 2 leads must score 35+ (own or with a code); the rest go to the dropped list; £1/100%/1k scores 80; Under-£60 score: ROI and volume carry it (£1/100%/1k = 75); Amazon Basics, Clipper, Google blacklisted; duvets/bedding never-sell; Under-£60 score leans on ROI + profit a unit (Hogwarts 33→53); Suz · Amazon deep drops £10–40 seeded with the merged filter; A Rule 2 source takes several UK exports and merges them de-duped by ASIN; £/month figures gone (Demand sub-line, summary tile → ROI 20%+); Grocery/beauty rows get Boots/Superdrug/Tesco code potential; Marks Electrical/AO only offered on real electricals; BLS removed everywhere; All Testing sources go Active; list sorts due-now first; Runs log redesigned; stale summary cleared when opening a source with nothing to show; Keepa token balance in the header via the Cloudflare Worker (bdl-sourcing); Floors card moves under the run summary (no dead space), "Tanked" chip reads "FBA price falling"; A run source opens on its saved leads (fresh export re-runs); retailer names in full; brighter OA cell; link buttons; SPM floor wording; VAT rule is now called Rule 3 everywhere on screen (was Rule 4; the old Rule 3 folded into Rule 2 on 13 Sep); Electronics/computers/large appliances take the plateau model under £60 (Canon TS4150i); FBA floor = lower of the FBA 30d/90d averages; OA cell names the brand code (Shark 9%, Hoover 15%…); Rule 2 £60+ refit; Keepa inputs on lead states; who-are-you gate
  slot and no Step-4 merge box; OA chips just say 'Check OA · up to x% ROI' (retailers on hover).
Previous: v1.1 · 2026-09-14 · b20 — Rule 1 leads get a score (same formula, display only, the frozen maths is untouched) and sort by it;
  flag chips are short labels with the full text on hover; buy market shown as a flag; calmer palette (no coloured pills, ROI green/amber/red);
  dropped-reason rows list their ASINs on hover; OA chip only when it is worth ≥8% ROI.
Previous: v1.1 · 2026-09-14 · b19 — low-ticket sell: the 180d Buy Box average is ignored when it is a different price regime (>1.35× the 90d,
  e.g. a launch price) — L'OR pods £19.94 → £10.82 (a loss, dropped); low-ticket SCORE scale: £1,500/month and £6/unit saturate under £60
  (WoodWick candle 28 → 51). Mera and all 12 grocery calls unchanged. S&S 136 leads.
  Also b19 — run screen tightened: floors live in the drop card (no dead space on one-file runs), summary compressed,
  Keepa/SAS/Buy/Sell links under the product title (Links column gone), score + status in one column, 'OA check · you check' column with
  category-aware retailers (grocery: Boots/Superdrug/Tesco by eye; electricals: Currys/AO/Marks Electrical; general: Argos/JL/Robert Dyas).
Previous: v1.1 · 2026-09-14 · b18 — under-£60 sell model refitted on 13 of Jack's grocery calls (higher Buy Box 90/180d average, no uplift,
  capped at the FBA 90d average; all calls within ±9%): S&S 137 leads, Business 10, Mera unchanged.
  Round two of Jack's calls: Logitech within 2% of the frozen Rule 1 (no change); high-ticket: only FBA history proves a plateau now
  (FBM-only rows get +5%, not +25%) — Galaxy Book4 Pro £2,207 → £1,788; Mera 12 Sep 390 → 376, 11 Sep unchanged.
  Also b18: Rule 3 folded into Rule 2 (Jack: behaviours belong to the product, not the filter): the under-£50 sell
  model, S&S / Business / coupon buy adjustments and Rule 4 VAT all apply per row on every UK filter. Sources on rule 3 migrate to 2.
Previous: v1.1 · 2026-09-14 · b17 — RULE 3 SELL CHANGE (Jack's grocery calls): under £50 the sell price is the lower Buy Box 90/180-day average
  with no plateau uplift, capped at the Amazon 90-day average; £50+ keeps Mera's model. S&S 596 → 103 leads, Business 16 → 8. Rule 2 untouched.
  Never-sell categories (alcohol, fashion, shoes — editable in Settings, shared) drop leads on every rule. HP + Oral-B in the brand blacklist (live).
Previous: v1.1 · 2026-09-14 · b16 — run summary card now carries the story, the status pills and a detail block (why things dropped, leads by
  market or score band, ROI bands) so there is no dead space; flag links tick green once that country's export is in; every row has Buy <market>
  and Sell UK links; floors save quietly (no toast, re-log 2.5 s after typing stops, 0 = unset); 'Open all N in Keepa' when the view is ≤250;
  list: on-it tag under the status, 'manual export' instead of a token count, hover highlight.
Previous: v1.1 · 2026-09-14 · b15 — UK-buy Rule 1 leads carry an 'OA · Currys 5% → £x · y% ROI' chip (best price-matcher from the discount
  list, all of them on hover) plus the Currys/Argos/JL/Brand-direct confirm toggles; tables use Inter instead of the monospace (Jack: 'font is a bit
  shit'); links as a 2×2 grid; sales floor placeholder shows the rule's own 10/month.
Previous: v1.1 · 2026-09-14 · b14 — leads table redesign: ASIN sits under the title (both rules fit the card without scrolling at 1440),
  coloured market pills, ROI bands, coloured flags, tinted header, row hover; the story sits inside the Run summary card (no dead space);
  floors bar renamed 'Customise this source'.
Previous: v1.1 · 2026-09-14 · b13 — Rule 1 run screen: the merge/Viewer bar is visible from the start ('Step 4 happens here'), plain-text
  note that Keepa cannot take the marketplace from a link (switch the flag in Keepa per country); story repaints after verdicts; No rows readable.
Previous: v1.1 · 2026-09-14 · b12 — leads per page (50/100/200/all) with 'Open these N in Keepa' saying how many it opens; the run
  summary points at 'Mark all as seen' when unreviewed leads are carried over from the last run.
Previous: v1.1 · 2026-09-14 · b11 — readability + density pass on the leads table (brighter greys, bigger secondary text, tighter rows,
  chips capped at 4 + '+n'), Track button removed (Keepa's page has the Track tab), links next to the verdict, run summary told as a
  sentence (export → leads → to review today, and what changed vs last time), own lock never shown as 'someone else'.
Previous: v1.1 · 2026-09-13 · b10 — shared storage (Supabase src_ tables, local-first outbox), who-am-I dropdown, per-source in-progress lock,
  To-review queue (new + better since last verdict, 2p counts), ASIN blacklist with reason, brand blacklist with Jack approval,
  Rule 4 VAT (tea/coffee 0%, VA override chip, editable words), Rule 3 = Suz grocery/S&S/Business (same maths as Rule 2),
  Suz's three filters seeded (EU one parked, tea & coffee waiting for its link), per-source FLOORS (the customisable Filter & Sort,
  saved on the source, applied before the queue), brand + title stored with every lead, Settings regrouped with shared / this-browser tags,
  Track-at-£ note, Keepa 'low was' on WORSE rows. Checks: 41.
  Rule 3 locked on Suz's 13 Sep exports (S&S 2,520 → 596 · Business 179 → 16); Rule 4 word lists widened (syrups, perfumes, cosmetics, supplements = 20%). Checks: 54.
  Edit drawer redesigned 13 Sep 22:10: sections (What it is / Where it buys / Who and when / Keepa / Notes), rule + status as pick-one cards,
  owner + cadence one-tap, market chips, link field shows saved vs generated with Open, brand-only and filter-only fields hide, Delete in the footer.
  LIVE-TESTED 13 Sep 21:30 with two browsers against the real src_ tables (seed rows are in; test rows removed).
Previous: v1.0 · 2026-09-13 · b9 — Jack's real Keepa Finder links seeded for 15 sources; Corsair/Elgato, Ninja/Shark, SanDisk/Seagate/WD merged; Bialetti + Repken seller watch added; UK-only brand runs work off one Finder export
