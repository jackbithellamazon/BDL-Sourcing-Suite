BDL SOURCING — b73 · 16 Sep 2026

ONE STEP. The Cloudflare Worker is already done (you deployed it, /health says hasKey true,
charts pull as real PNGs and cache for 24h). Nothing to do there.

1. UPLOAD THE APP
   GitHub → your sourcing repo → Add file → Upload files.
   Drag in: index.html, css, js, tests, README-DEPLOY.txt  (everything except this file).
   Commit. GitHub Pages serves it within a minute, same URL.
   Then hard-refresh the app once (Cmd+Shift+R).

WHAT CHANGED SINCE b72
   The SellerAmp button no longer sends a cost price.
   The Buy Box figure on the audit panel is what the RIVAL sells at, not what you pay, so
   sending it as sas_cost_price made SellerAmp open on a 0% deal. It now opens on the ASIN
   alone — exactly what pressing S already did, so the button and the key finally match.
   A check guards it: "Audit · SellerAmp opens on the ASIN alone, no cost price".

ROLLBACK
   Upload the previous files over the top. GitHub keeps every version in the repo history.

95 checks, all green.
