BDL SOURCING — b156 · 25 Sep 2026

This folder is the whole app. Upload all of it every time; anything you already pushed is simply replaced.

1. UPLOAD
   GitHub → Add file → Upload files. Drag in index.html, css, js, tests, README-DEPLOY.txt. Commit.
   Hard-refresh once (Cmd+Shift+R) — the header should say b156.

2. SIGN YOURSELF IN (no email, no Supabase step)
   Settings → the "Who am I" box → jack@bdl.local + your usual password → Sign in.
   Your Team row already says jack@bdl.local.

3. LINKS FOR SUZ AND MERA — ONCE
   a) Supabase → Authentication → Users → Add user → CREATE NEW USER, one for each of them.
      Any email - it does NOT need a real inbox (e.g. suz@bdl.local, mera@bdl.local).
      Any password (nobody types it). Tick "Auto Confirm User".
   b) App → Settings → Team: type the same email in Suz's row and Mera's row.
   c) Cloudflare → Workers & Pages → bdl-sourcing → Edit code: select all, paste the whole of
      ~/Downloads/2026-09-25-BDL-SOURCING-WORKER-v3-PASTE-THIS.js, Deploy.
      (It is your v2 exactly, plus the link-maker. The Keepa key is not in it and does not change.)
   d) bdl-sourcing → Settings → Variables and Secrets → Add:
        SUPABASE_URL                Type: Text    Value: Supabase → Project Settings → Data API → Project URL
        SUPABASE_SERVICE_ROLE_KEY   Type: SECRET  Value: Supabase → Project Settings → API Keys → service_role (Reveal, copy)
      The service_role key is the master key of the project. It goes ONLY there, as a Secret - never in a chat or a file.
      Deploy again if it asks.

4. EVERY TIME SOMEONE NEEDS IN
   Settings → Team → "Copy link" next to their name → paste it to them in Discord.
   They open it on their WORK computer and they're signed in (it signs in whichever browser opens it).
   Each link works ONCE, for about an hour. Need another? Press Copy link again.

5. THE LOCK
   When all three show "Signed in", flip "Sign-in required for everyone".
   Not signed in = the sign-in screen and nothing else. Their name comes from the sign-in.

GUEST MODE (b155) - RUN EVERYTHING, SAVE NOTHING
   Settings > Guest mode > Start guest mode. Amber bar = nothing goes to Supabase.
   Leave guest mode = everything you did is thrown away and this browser goes back exactly as it was.
   Keepa API buttons still spend real tokens. Sign-in links and the lock are paused inside it.

WHAT LANDS, NEWEST FIRST
   b156 SIGN IN YOUR WAY + COPY-A-LINK + A CALMER BRANDS LIST.
        You: email + password, straight in. Suz and Mera: a link you copy and send - no email.
        Brands list: rows no longer one maroon wall (a clear red edge on late ones instead), each
        owner shows in their own colour (Jack violet, Suz blue, Mera orange, VAs pink - none of
        them now clash with "late" red or "to review" amber), "44 to review" is an amber chip, and
        only Run is a filled button. Tested: 275 checks.
   b155 GUEST MODE.
   b154 Due / late in plain words, Lead / Not a lead in the Keepa console.
   b153 The lock, Mac type, nothing moves when you click, bulk audit.
